# 开发任务

读取以下产品文档：

{文档路径}

F:\code\project\NX9\docs\NX9-BOTTOM-WORKSPACE-REFACTOR-SPEC-v1.md

你的所有开发工作，必须100%以该文档为唯一开发依据。

--------------------------------------------------
【最高优先级规则】
--------------------------------------------------

1、严禁自行设计方案。

如果文档已经规定了：

- 功能
- UI
- 流程
- 页面
- 字段
- 节点
- 数据结构
- 接口
- 状态
- 文案
- 交互

则必须完全按照文档实现。

不得：

- 自己优化
- 自己修改
- 自己简化
- 自己增加
- 自己删除
- 自己替换
- 自己发挥

哪怕你认为更合理，也禁止修改文档方案。

--------------------------------------------------
【必须实现】
--------------------------------------------------

凡是文档中明确要求实现的内容：

无论大小，
无论复杂程度，
无论涉及多少文件，

都必须实现。

不得：

- 遗漏
- 跳过
- 留 TODO
- 留占位代码
- 留空组件
- 留假数据（除非文档要求）
- 留未完成逻辑
- 只实现一半

必须实现完整功能。

--------------------------------------------------
【可选功能】
--------------------------------------------------

如果文档中出现：

可选
Optional
建议
后续实现
未来支持
可以增加
可考虑

这一类描述，

禁止直接开发。

必须先整理列表：

可选功能：

1.
2.
3.

然后询问我：

"以上属于文档中的可选功能，是否需要实现？"

只有得到明确同意后，
才能开始开发。

--------------------------------------------------
【不明确内容】
--------------------------------------------------

如果文档存在：

- 歧义
- 描述不足
- 缺少参数
- 缺少流程
- 缺少字段
- 缺少接口说明
- 缺少状态说明
- 无法确定实现方式

禁止猜测。

禁止按照经验实现。

禁止参考其它项目实现。

禁止打开、阅读或对照 `Reference_Projects/` 及任何外部“原项目 / 对标项目”。

NX9 按独立自研产品推进；不得以“对齐某参考仓库”作为设计或实现依据。

必须立即停止开发，

直接向我提问。

得到回答后再继续。

--------------------------------------------------
【禁止行为】
--------------------------------------------------

未经文档说明，

禁止：

新增功能

删除功能

修改业务逻辑

修改页面布局

修改UI设计

修改交互流程

修改接口定义

修改数据库结构

修改目录结构

修改命名规范

修改状态管理

修改组件职责

修改文档

删除已有代码

重构已有代码

替换技术方案

更换第三方库

优化性能

优化代码风格

优化架构

任何超出文档范围的修改都禁止。

--------------------------------------------------
【已有代码】
--------------------------------------------------

实现新功能时：

优先复用已有代码。

不得因为个人习惯：

整体重写

大面积重构

替换已有实现

除非文档明确要求。

--------------------------------------------------
【代码质量】
--------------------------------------------------

所有新增代码必须：

保持项目现有代码风格；

遵循已有目录结构；

遵循已有命名规范；

保证可以正常编译；

保证无明显错误；

保证类型正确；

保证引用完整；

不得产生新的报错。

--------------------------------------------------
【开发流程】
--------------------------------------------------

严格按照以下流程：

第一步：

阅读完整文档。

第二步：

整理所有需要实现的功能。

第三步：

检查项目当前实现情况。

第四步：

列出：

已完成

未完成

部分完成

第五步：

制定开发计划。

第六步：

开始开发。

第七步：

完成后再次全文检查文档。

确认没有遗漏任何必须实现的内容。

--------------------------------------------------
【最终检查（必须执行）】
--------------------------------------------------

开发结束以后，

再次逐条对照文档，

输出：

✅ 已完成

❌ 未完成

⚠ 存在疑问

并说明原因。

直到确认：

文档中所有必须实现的内容全部完成。

--------------------------------------------------
【输出要求】
--------------------------------------------------

所有回复必须使用简体中文。

不要省略任何实现步骤。

不要自行结束任务。

不要因为功能较多而跳过。

如果一次无法完成，请持续开发下一部分，直到文档中的所有必须实现功能全部完成。

始终牢记：

文档是唯一事实来源（Single Source of Truth）。

任何代码实现，都必须严格遵循文档，不得擅自发挥。

【零容忍规则】

出现以下任意一种行为，均视为违反开发要求：

1. 未实现文档中的任何一个必须功能；
2. 擅自新增文档不存在的功能；
3. 擅自修改文档规定的实现方式；
4. 擅自优化、重构或替换已有代码；
5. 对不明确内容进行猜测实现；
6. 未经确认实现任何可选功能；
7. 为了节省时间省略实现细节；
8. 使用 TODO、占位代码、Mock、假逻辑替代正式实现（文档明确允许的除外）；
9. 未完成全部要求就结束任务；
10. 声称"已完成"，但未逐项对照文档进行最终核查。

若遇到任何不确定之处，唯一允许的行为是暂停开发并向我提问，禁止猜测、禁止默认、禁止自行决定。