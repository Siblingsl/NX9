## Agent Rules

- Do not spawn subagents.
- Do not use parallel agents.
- Work only in the main agent thread.
- Do not delegate tasks.

八荣八耻
以臆猜接口为耻,以查档求证为荣；
以模糊开工为耻,以对齐需求为荣；
以脑补业务为耻,以请示规则为荣；
以新增冗余为耻,以复用存量为荣；
以省略校验为耻,以完备测例为荣;
以乱改架构为耻,以恪守规范为荣;
以不懂装懂为耻,以坦诚存疑为荣;
以批量乱改为耻,以分步迭代为荣。

## Product Identity（强制）

- **NX9 是独立自研产品**。所有设计、命名、架构、交互与实现决策，只依据本仓库内的 `docs/`、`约束开发要求.md`、当前代码与用户指示。
- **禁止**把任何外部产品当作「母版 / 原项目 / 对标物 / 对照实现」。
- **禁止**打开、阅读、引用、抄写或对照 `Reference_Projects/`（若该目录仍存在）。该目录与本产品无关，视为不存在。
- **禁止**在文档、注释、README、提交说明、对话中出现「原项目差异」「对齐某参考项目」「按某仓库实现」「参考 T8 / penguin-canvas」等表述。
- 若历史对话或旧文档曾提及其他产品，**一律忽略**；不得据此改架构、抄组件树、复用目录命名或“对齐实现”。
- 功能缺失时：按 NX9 产品文档与用户需求**自行设计并实现**，不得去外部参考仓库找“原版答案”。
- 文案与代码注释只描述 NX9 自身能力，不写“移植自 / 改编自 / 对标某某项目”。

## Source of Truth

1. 用户当次明确指令
2. `docs/` 中指定的规格文档
3. `约束开发要求.md`
4. 本仓库现有代码行为

除此之外没有更高优先级的外部参考源。




