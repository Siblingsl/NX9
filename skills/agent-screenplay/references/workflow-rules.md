# 工作规则

1. 先判定：首发生成 / 续写一集 / 重写一集。
2. 锁定名册与体例，再拆场写作。
3. 场景头唯一形态：`## Sxx | 内景/外景 · 地点 | 时间`。
4. 对白唯一形态：`角色名：台词` 或 `角色名（状态）：台词`（无引号）。
5. 续写必须衔接上一集状态；重写不得与邻集矛盾。
6. 只输出纯文本；失败时也不要用 JSON 顶替。
7. 非终章禁止 `（完）`；集末留钩子。
