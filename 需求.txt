1.场景库也需要上传参考图（多个）。
2.我觉得把这个移到画布的中间顶部区域，我感觉应该在用户选择什么模式，就展示对应的流程，例如ai漫剧-真人就是13个步骤，这里就展示13个对应步骤，每个对应的步骤要对应节点（要检测完成状态，未完成要打感叹号），如果用户选择自由模式，这里就展示自由模式，
3.13步骤的使用了也都不是连贯了，连接线有的没有通，而且节点的顺序也都是乱的，没有1-13依次向后。




剧本导入
      │
      ▼
AI 剧本拆解
      │
      ▼
剧本预览（第一集 / 第二集 / ……）
      │
      ▼
分镜设计（Storyboard）
      │
      ▼
分镜图片预览（Storyboard Preview）
      │
      ├── 根据剧本自动计算需要多少张分镜图
      ├── 根据镜头自动生成每张分镜图
      ├── 时间轴展示（例如 0s、5s、10s……）
      ├── 支持单张重新生成
      ├── 支持编辑某一张 Prompt
      ├── 支持替换参考图
      ├── 支持增加一个分镜
      ├── 支持删除一个分镜
      ├── 支持拖拽调整顺序
      ├── AI 检查角色一致性
      ├── AI 检查场景一致性
      └── 全部确认
      │
      ▼
视频生成
      │
      ├── 自动引用所有分镜图片
      ├── 自动引用对应 Prompt
      ├── 自动引用对应镜头
      ├── 首尾帧（模型支持时）
      └── 正式生成视频
      │
      ▼
视频预览
      │
      ▼
导出


分镜预览功能拓展：
预留但暂不实现：

- AI 自动补帧建议
- AI 镜头节奏优化
- AI 自动调整 Shot 长度
- AI 自动删减重复镜头
- AI 推荐转场
- AI 推荐运镜
- AI 推荐 BGM
- AI 自动生成镜头说明
- AI 自动生成导演备注
- AI 自动生成字幕建议

这些功能只预留扩展接口，不进入本期开发。