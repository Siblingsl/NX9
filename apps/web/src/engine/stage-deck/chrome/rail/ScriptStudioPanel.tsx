import { FileText } from 'lucide-react';
import { useActivityLog } from '../../../../stores/activity-log';

/**
 * P0：Rail 五步向导收口（已隐藏）。
 * O-9：P0 隐藏，不再展示；P1 时可删除此文件。
 */
export function ScriptStudioPanel() {
  return null;
}
