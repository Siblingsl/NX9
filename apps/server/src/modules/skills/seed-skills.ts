/** Seed skills written on first boot. NX9-native methodology files
 *  (cinema prompt, storyboard, script rewrite, voice assignment, prompt polish).
 *  Never overwrites existing skill files. */
export interface SeedSkill {
  id: string;
  content: string;
}

export const SEED_SKILLS: SeedSkill[] = [
  {
    id: 'script-skeleton',
    content: `---
name: 故事骨架
description: 分析小说/章节，输出三幕结构、分集数、付费卡点
---

# 故事骨架

## 输入
一段故事或小说章节文本。

## 输出格式
结构化 JSON，包含以下字段：

- **title**：故事标题
- **logline**：一句话梗概（25 字内）
- **acts**：幕列表，每幕含 name + beats（节拍）
- **episodeCount**：推荐分集数
- **hookPoints**：付费/悬念卡点列表

## 分幕原则
1. 三幕结构：建置（25%）→ 对抗（50%）→ 解决（25%）
2. 短剧（≤3min/集）每集 1 个核心情节点
3. 长篇连续剧每集 2-3 个情节点，结尾留钩子
4. 每幕 2-5 个节拍（beat），节拍是推动剧情的最小单元
`,
  },
  {
    id: 'production-storyboard-table',
    content: `---
name: 分镜表
description: 根据导演规划/剧本生成可执行分镜表，每行对应一个镜头
---

# 分镜表

## 输入
导演规划或剧本文本。

## 输出格式
JSON 数组，每行一个镜头：

{"id":"唯一ID","group":"S01","shotSize":"CU","cameraMove":"推","durationSec":4,"descriptionZh":"画面描述","dialogue":"对白","sfx":"音效","videoDesc":"视频动态描述","associateAssetIds":[]}

## 分镜规则
1. **一个镜头一个动作**：不把多个动作塞进一镜
2. **首镜定调**：第一镜确立空间、氛围、主体
3. **情绪转折分镜**：每个情绪转折点断一次
4. **运镜可执行**：只写能生成的运镜（推、拉、摇、移、跟、固定）
5. **连续性**：相邻镜头的主体位置、光线、服装保持一致
6. **时长合理**：对话 3-4s，动作 4-6s，环境 4-6s
7. **S-Class 分组**：单组 ≤15s，场景变化处断开
`,
  },
  {
    id: 'script-adaptation',
    content: `---
name: 改编策略
description: 根据故事骨架和原文，制定改编策略（调性、节奏、删减规则）
---

# 改编策略

## 输入
- 故事骨架（StorySkeleton）
- 原文文本

## 输出格式
结构化 JSON，包含以下字段：

- **sourceType**：原文类型（novel/outline/script）
- **tone**：调性（如"轻松搞笑""悬疑紧张""温情治愈"）
- **pacing**：节奏（fast/medium/slow）
- **omitRules**：删减规则列表（如"删内心独白""合并配角"）
- **emphasis**：强调要素列表（如"强化第一集钩子""保留高燃台词"）

## 改编原则
1. 保留核心冲突和人物弧光
2. 叙述转为可拍摄场景
3. 内心独白转为表情/动作或画外音
4. 长段落拆为多场短戏（每场 30-60s）
5. 角色语言风格保持一致
`,
  },
  {
    id: 'script-screenplay',
    content: `---
name: 分集剧本
description: 根据改编策略和章节内容，输出格式化分集剧本
---

# 分集剧本

## 输入
- 改编策略（AdaptationStrategy）
- 章节原文

## 输出格式
Markdown，每集以 \`## E01\` 开头：

\`\`\`
## E01 | 集标题

场景：内/外 · 地点 · 时间

动作描写（自然段，不含镜头语言）。

角色名：（状态/表情）台词内容
\`\`\`

## 格式规则
- \`## E编号 | 集标题\` 为集头
- \`场景：\` 为场景头，格式固定
- 动作描写用自然段
- 对白格式：\`角色名：（表情）台词\`
- 每集 5-10 场，每场 30-60 秒

## 原则
1. 不写镜头语言（景别、角度、运镜）
2. 心理描写转为表情/动作或画外音
3. 角色语言风格保持一致
4. 每集结尾留悬念/钩子
`,
  },
  {
    id: 'production-director-plan',
    content: `---
name: 导演规划
description: 根据剧本生成导演规划（场景调度、镜头语言、视觉风格）
---

# 导演规划

## 输入
分集剧本文本（Markdown 格式）。

## 输出格式
Markdown，每场戏一个段落：

### 场景1 | 内·咖啡馆·黄昏

**调度**：人物 A 坐窗边 → B 推门入 → A 抬头

**镜头**：从 A 的过肩看 B → 反打 B 中景 → 双人全景

**视觉**：暖色黄昏光，浅景深，手持轻微晃动

## 规划原则
1. 每场标注调度（blocking）、镜头方案、视觉参考
2. 对话场景用 180 度原则布局
3. 动作场景标注关键姿态（起→高潮→收）
4. 给出整体视觉风格参考（色板/光影关键词）
`,
  },
  {
    id: 'cinema-prompt',
    content: `---
name: 电影感提示词
description: 把普通描述改写为电影摄影风格的专业提示词（景别、运镜、光线）
---

# 电影感提示词生成

## 目标
把用户给的画面描述，改写成专业摄影语境下的提示词，让图像/视频生成结果更具电影质感。

## 五要素
每次输出都覆盖这五个维度，顺序为：**主体 → 景别 → 运镜 → 光线 → 风格**。

1. **主体**：核心人/物/场景，保留用户意图
2. **景别**：特写 / 中景 / 全景 / 远景（shot size）
3. **运镜**：推 / 拉 / 摇 / 移 / 跟 / 升降 / 环绕（camera movement）
4. **光线**：顺光 / 逆光 / 侧光 / 顶光 / 黄昏暖调 / 蓝调时刻
5. **风格**：胶片质感 / anamorphic / 手持纪录片 / 韦斯·安德森 / 赛博朋克

## 输出格式
直接输出一段英文提示词，逗号分隔，不解释。例如：
\`close-up of a woman reading by the window, slow dolly-in, soft side light, golden hour, anamorphic film look, shallow depth of field\`
`,
  },
  {
    id: 'storyboard-breaker',
    content: `---
name: 分镜拆解
description: 把一段剧本/故事拆成可逐镜生成的分镜表（场景、镜头、提示词、时长）
---

# 分镜拆解

## 输入
一段故事或剧本文本。

## 输出格式
Markdown 表格，每个镜头一行，控制每镜 2-3 秒（默认 3，一般不超过 4）：

| 镜号 | 景别 | 画面描述 | 英文提示词 | 时长 | 备注 |

## 拆解原则
1. **一个镜头一个动作**：不把多个动作塞进一镜
2. **首镜定调**：第一镜确立空间、氛围、主体
3. **情绪转折分镜**：每个情绪转折点断一次
4. **运镜可执行**：只写 AI 视频/图像能生成的运镜（推、拉、摇、移、跟、静止）
5. **连续性**：相邻镜头的主体位置、光线、服装保持一致
6. **时长合理**：人物对话镜 3-4s，环境交待镜 4-6s

## 提示词要求
英文提示词列必须自包含（self-contained），可直接喂给生成模型，包含主体 + 场景 + 光线 + 风格。
`,
  },
  {
    id: 'script-rewriter',
    content: `---
name: 剧本改写
description: 把小说/叙事文字改写成格式化剧本（场景头 + 动作 + 对白）
---

# 剧本改写指南

## 改写原则
1. **保留核心情节**：不改变主线和角色关系
2. **增强画面感**：叙述转为可视化场景描写
3. **对话驱动**：用对白推动情节，减少旁白
4. **节奏把控**：每场戏 30-60 秒，适合短视频
5. **不写镜头语言**：景别、角度、运镜交给分镜步骤

## 格式
\`\`\`
## S01 | 内景 · 咖啡厅 | 黄昏

动作描写（自然段，不含镜头语言）。

角色名：（状态/表情）台词内容
\`\`\`

## 格式规则
- \`## S编号 | 内景/外景 · 地点 | 时间段\` 为场景头
- 动作描写用自然段
- 对白格式：\`角色名：（表情）台词\`

## 注意
- 心理描写转为表情/动作或画外音
- 长叙述拆成多场短戏
- 角色语言风格保持一致
- 场景编号连续递增（S01, S02…）
- 时间段要具体（黄昏、深夜、清晨），不写笼统的"白天"
`,
  },
  {
    id: 'voice-assigner',
    content: `---
name: 角色配音分配
description: 根据角色设定推荐合适的 TTS 音色与语速情绪参数
---

# 角色配音分配

## 任务
为剧本中每个角色推荐一组配音参数：**音色（voice）+ 语速（speed）+ 情绪（emotion）**。

## OpenAI 兼容音色池
alloy · echo · fable · onyx · nova · shimmer
（若使用其他 TTS 提供方，请改用对应音色 ID）

## 判断维度
1. **性别年龄**：对齐音色性别与年龄段
2. **性格**：沉稳/活泼/阴郁/温柔 → 对应低沉/明亮/沙哑/柔和音色
3. **场景情绪**：紧张戏加快语速，舒缓戏减慢

## 输出格式
Markdown 表格：

| 角色 | voice | speed | emotion | 理由 |

## 原则
- 一部戏内同一角色始终用同一音色（声音一致性）
- 旁白/画外音与角色音色区分开
- speed 范围 0.75–1.25，默认 1.0
- 给出简短理由，便于人工微调
`,
  },
  {
    id: 'storyboard-sketch',
    content: `---
name: 线稿分镜
description: 把分镜表输出为含「构图要点」列的可执行线稿分镜表，供 AI 生成线稿或手绘使用
---

# 线稿分镜

## 输入
一段故事或剧本文本，或已有文本分镜表。

## 输出格式
Markdown 表格，每个镜头一行，控制每镜 2-3 秒（默认 3，一般不超过 4）：

| 镜号 | 景别 | 画面描述 | 英文提示词 | 构图要点 | 时长 |

## 构图要点列规则
构图要点列必须包含以下维度的描述：

1. **主体位置**：人物在画面中的位置（左/中/右/黄金分割点）
2. **景别关系**：特写/中景/全景/远景，以及与前后镜头的景别衔接
3. **视线方向**：角色看的方向（左/右/镜头/偏离镜头）
4. **空间关系**：多人场景中各角色的相对位置和距离
5. **层次感**：前景/中景/后景的元素分布

## 线稿生成原则
1. 构图要点用中文简洁描述，每点 2-5 字即可
2. 相邻镜头的构图要点保持连续性（主体位置不能突变）
3. 对话镜头遵循「180 度规则」：角色视线方向一致
4. 动作镜头标注关键姿态（起势/动作高潮/收势）
5. 构图要点可直接拼接为 \`buildLineArtShotPrompt\` 的输入
`,
  },
  {
    id: 'prompt-polish',
    content: `---
name: 提示词润色
description: 把口语化、含糊的提示词润色为结构清晰、要素齐全的生成提示词
---

# 提示词润色

## 目标
把用户随口写的提示词，润色成结构清晰、要素齐全、可直接用于图像/视频生成的提示词。

## 五段结构
优秀提示词按以下顺序组织：

1. **主体**（Subject）：谁 / 什么，核心动作
2. **环境**（Environment）：场景、地点、背景
3. **光线**（Lighting）：时间、光质、光位
4. **风格**（Style）：艺术风格、镜头、质感
5. **参数**（Quality）：分辨率、细节强度、负面词

## 润色原则
- **去冗余**：删除"很漂亮、很好看"等无效词，换成具体描述
- **补缺失**：缺哪段补哪段，但不臆造用户没说的主体
- **英文化**：最终输出英文，逗号分隔
- **保留意图**：用户强调的元素必须保留并强化

## 输出
直接输出润色后的英文提示词，不解释。
负面词用 \`--no\` 或单独的 negative prompt 行给出（视下游模型而定）。
`,
  },
];
