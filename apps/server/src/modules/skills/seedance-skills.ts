import type { SeedSkill } from './seed-skills';

/**
 * Seedance methodology packs were removed from the default skill tree
 * until they are regenerated to the full §1.5 project template.
 * Optional import endpoint may stay, but ships empty by default.
 */
export const SEEDANCE_SKILLS: SeedSkill[] = [];
